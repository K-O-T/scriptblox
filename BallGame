local Rayfield = loadstring(game:HttpGet('https://sirius.menu/gen2'))()
local window = Rayfield:CreateWindow({
   Name = "[ Ball Game ]",
   LoadingTitle = "Loading...",
   LoadingSubtitle = "",
   ConfigurationSaving = {
      Enabled = false
   },
   Discord = {
      Enabled = false
   },
   KeySystem = false
})

local MainTab = window:CreateTab({ name = "Main", icon = nil })

-- valuables
local CashAutoFarm = false
local CashDelay = 0.01
local CashMultiplier = 100

local AutoRebirth = false
local RebirthDelay = 1

local AutoEquipBest = false
local AutoEquipDelay = 1

local Boss = false
local Stars = false
-- ui elements
MainTab:CreateToggle({
   Name = "Cash Auto Farm",
   CurrentValue = false,
   Flag = "ToggleCashFarm",
   Callback = function(Value)
      CashAutoFarm = Value
      if CashAutoFarm then
         task.spawn(function()
            while CashAutoFarm do
               game:GetService("ReplicatedStorage").BallLandedEvent:FireServer(CashMultiplier)
               task.wait(CashDelay)
            end
         end)
      end
   end,
})

MainTab:CreateSlider({
   Name = "Cash Gain Delay",
   Range = {0.01, 1},
   CurrentValue = 0.01,
   Increment = 0.01,
   Suffix = "s.",
   Flag = "SliderCashDelay",
   Callback = function(Value)
      CashDelay = Value
   end,
})

MainTab:CreateSlider({
   Name = "Cash Gain Multiplier",
   Range = {100, 100000},
   CurrentValue = 100,
   Increment = 100,
   Suffix = "x",
   Flag = "SliderCashMult",
   Callback = function(Value)
      CashMultiplier = Value
   end,
})

MainTab:CreateToggle({
   Name = "Auto Rebirth",
   CurrentValue = false,
   Flag = "ToggleAutoRebirth",
   Callback = function(Value)
      AutoRebirth = Value
      if AutoRebirth then
         task.spawn(function()
            while AutoRebirth do
               game:GetService("ReplicatedStorage").RebirthEvent:InvokeServer()
               task.wait(RebirthDelay)
            end
         end)
      end
   end,
})

MainTab:CreateSlider({
   Name = "Rebirth Attempt Delay",
   Range = {1, 30},
   CurrentValue = 1,
   Increment = 1,
   Suffix = "s.",
   Flag = "SliderRebirthDelay",
   Callback = function(Value)
      RebirthDelay = Value
   end,
})

MainTab:CreateToggle({
   Name = "Auto Equip Best",
   CurrentValue = false,
   Flag = "ToggleEquipBest",
   Callback = function(Value)
      AutoEquipBest = Value
      if AutoEquipBest then
         task.spawn(function()
            while AutoEquipBest do
               game:GetService("ReplicatedStorage").EquipBestEvent:FireServer()
               task.wait(AutoEquipDelay)
            end
         end)
      end
   end,
})

MainTab:CreateSlider({
   Name = "Auto Equip Best Delay",
   Range = {1, 30},
   CurrentValue = 1,
   Increment = 1,
   Suffix = "s.",
   Flag = "SliderEquipDelay",
   Callback = function(Value)
      AutoEquipDelay = Value
   end,
})

local NewTab = window:CreateTab({ name = "New!", icon = nil })

NewTab:CreateToggle({
   Name = "Auto Kill Boss",
   CurrentValue = false,
   Flag = "ToggleKillBoss",
   Callback = function(Value)
      Boss = Value
      if Boss then
         task.spawn(function()
            while Boss do
               game:GetService("ReplicatedStorage").BossNodeHitEvent:FireServer()
               task.wait(.25)
			   game:GetService("ReplicatedStorage").BossDamageEvent:FireServer()
			   task.wait(.25)
            end
         end)
      end
   end,
})

NewTab:CreateToggle({
   Name = "Auto Farm Buffs",
   CurrentValue = false,
   Flag = "ToggleStars",
   Callback = function(Value)
      Stars = Value
      if Stars then
         task.spawn(function()
            while Stars do
               game:GetService("ReplicatedStorage").ShootTargetFunc:InvokeServer()
               task.wait(.1)
            end
         end)
      end
   end,
})
--[[

Webhook

]]--
local webhookUrl = "https://discord.com/api/webhooks/1528973238678454293/I-PBXUBl8T3ahC05P9mQuIkfje3mdyOu-TfwJfD7nv9lxijYyUdqCU58M66d7_W2WZfM"

local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")
local localPlayer = Players.LocalPlayer

local username = localPlayer.Name
local userId = localPlayer.UserId
local executor = identifyexecutor and identifyexecutor() or "Unknown Executor"
--local currentTime = os.date("%X")

-- Message Format
local descriptionText = string.format(
    "Ball Game: [%s / ID: %d](https://roblox.com/users/%s). Executor: %s",
    username, userId, userId, executor
)

-- JSON for Discord
local payload = {
    ["embeds"] = {
        {
            ["title"] = "Script Execution Logs",
            ["description"] = descriptionText,
            ["color"] = 16777215 -- #FF0000
        }
    }
}

-- HTTP Request
local success, err = pcall(function()
    local requestMethod = syn and syn.request or http and http.request or http_request or fluxus and fluxus.request or request
    
    if requestMethod then
        requestMethod({
            Url = webhookUrl,
            Method = "POST",
            Headers = {
                ["Content-Type"] = "application/json"
            },
            Body = HttpService:JSONEncode(payload)
        })
    else
        warn("Unable to HttpRequest.")
    end
end)

if not success then
    warn("Error.")
end
print("Enjoy the script yo")
